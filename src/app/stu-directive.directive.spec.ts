import { StuDirectiveDirective } from './stu-directive.directive';

describe('StuDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StuDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
